import styled from 'styled-components';

export const Card = styled.div`
  display: flex;
  background: #fff;
  border-radius: 10px;
  box-shadow: 0 0 1rem rgba(0, 0, 0, 0.15);
  margin: 1rem 0;
  padding: 6rem;
`;
