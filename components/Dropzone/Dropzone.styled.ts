import styled from 'styled-components';

export const Container = styled.div`
  display: flex;
  border: 3px dashed;
  border-radius: 5px;
  cursor: pointer;

  :hover {
    background-color: lightgray;
  }
`;
